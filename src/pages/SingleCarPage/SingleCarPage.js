import React from 'react';
import {useParams} from "react-router-dom";

const SingleCarPage = ({state}) => {
    const {carId} = useParams();
    return (
        <div>

        </div>
    );
};

export default SingleCarPage;